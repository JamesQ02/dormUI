<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dormandrentershub">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DormandRentersHub">
        <activity android:name=".OwnerRegistration"></activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".Messaging" />
        <activity android:name=".Dorm4" />
        <activity android:name=".Dorm3" />
        <activity android:name=".Dorm2" />
        <activity android:name=".Dorm1" />
        <activity android:name=".Settings" />
        <activity android:name=".UserChoice">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>